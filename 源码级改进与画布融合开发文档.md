```python
import os

md_content = """# LumiWeave 源码级改进与画布融合开发文档

在将 LumiWeave 从纯对话交互 (Conversational) 升级为 **“对话 + 画布”双轨融合模式 (Dual-Track UI)** 的过程中，需要在现有的前后端分离架构上进行针对性地重构。以下是具体的源码改进指导。

---

## 1. 前端架构优化 (`frontend/src/`)

前端需要引入基于节点的可视化库（强烈推荐 `React Flow`），并结合全局状态管理工具（如 `Zustand`）来维护画布中复杂的节点拓扑关系。

### 1.1 依赖引入
在 `frontend/package.json` 中新增依赖：
```bash
npm install reactflow zustand @xyflow/react

```

### 1.2 组件结构调整

修改现有的单列对话布局，采用抽屉式或左右分栏布局。

* **`frontend/src/App.tsx` 改造**：
将原本全屏的 `ChatPanel` 限制在左侧或作为悬浮窗，右侧引入新的 `CanvasPanel`。
```tsx
// 伪代码示例
import { ChatPanel } from './components/ChatPanel';
import { WorkflowCanvas } from './components/WorkflowCanvas';
import { useLayoutStore } from './store/layoutStore';

export default function App() {
  const { isCanvasOpen } = useLayoutStore();

  return (
    <div className="flex h-screen w-full bg-gray-50">
      {/* 左侧对话栏 */}
      <div className={`transition-all duration-300 ${isCanvasOpen ? 'w-1/3' : 'w-full max-w-3xl mx-auto'}`}>
        <ChatPanel />
      </div>

      {/* 右侧画布栏 */}
      {isCanvasOpen && (
        <div className="flex-1 border-l border-gray-200">
          <WorkflowCanvas />
        </div>
      )}
    </div>
  );
}

```



### 1.3 新增画布核心组件 (`frontend/src/components/WorkflowCanvas.tsx`)

实现节点的拖拽、连线以及自定义节点注册。
为了与后端 `renderers/comfyui.py` 等模块配合，节点设计可以直接对标这类渲染工具的节点结构。

```tsx
import React, { useCallback } from 'react';
import ReactFlow, { Background, Controls, addEdge, applyNodeChanges, applyEdgeChanges } from 'reactflow';
import 'reactflow/dist/style.css';
import { useWorkflowStore } from '../store/workflowStore';

// 自定义节点类型（如 LLM推理、知识检索、API渲染调用）
const nodeTypes = {
  llmNode: LLMCustomNode,
  renderNode: RendererCustomNode, 
};

export function WorkflowCanvas() {
  const { nodes, edges, setNodes, setEdges, onNodesChange, onEdgesChange } = useWorkflowStore();

  const onConnect = useCallback((params) => setEdges((eds) => addEdge(params, eds)), [setEdges]);

  return (
    <ReactFlow
      nodes={nodes}
      edges={edges}
      onNodesChange={onNodesChange}
      onEdgesChange={onEdgesChange}
      onConnect={onConnect}
      nodeTypes={nodeTypes}
      fitView
    >
      <Background />
      <Controls />
    </ReactFlow>
  );
}

```

---

## 2. 后端架构优化 (`backend/app/`)

后端的核心任务是接收前端传来的 DAG（有向无环图）JSON 数据，解析执行顺序，并提供实时状态反馈。这与 ComfyUI 等工具的底层执行逻辑非常相似。

### 2.1 增加数据模型 (`backend/app/agent/types.py`)

定义画布节点和边的数据模型，便于通过 FastAPI 接收。

```python
from pydantic import BaseModel
from typing import List, Dict, Any, Optional

class WorkflowEdge(BaseModel):
    id: str
    source: str
    target: str
    sourceHandle: Optional[str] = None
    targetHandle: Optional[str] = None

class WorkflowNode(BaseModel):
    id: str
    type: str
    data: Dict[str, Any]  # 存放节点的具体配置，如 prompt, model_name, render_params

class WorkflowGraph(BaseModel):
    nodes: List[WorkflowNode]
    edges: List[WorkflowEdge]

```

### 2.2 构建 DAG 执行引擎 (`backend/app/agent/engine.py` - 新增)

当前的 `router.py` 主要处理线性或基于大模型的单点路由。需要补充一个 `WorkflowEngine` 来按拓扑排序执行图结构。

```python
import networkx as nx
import asyncio
from app.agent.types import WorkflowGraph

class WorkflowEngine:
    def __init__(self, graph: WorkflowGraph):
        self.graph = graph
        self.dag = self._build_dag()

    def _build_dag(self) -> nx.DiGraph:
        G = nx.DiGraph()
        for node in self.graph.nodes:
            G.add_node(node.id, data=node.data, type=node.type)
        for edge in self.graph.edges:
            G.add_edge(edge.source, edge.target)
        if not nx.is_directed_acyclic_graph(G):
            raise ValueError("Workflow is not a valid DAG")
        return G

    async def execute(self, websocket=None):
        # 拓扑排序决定执行顺序
        execution_order = list(nx.topological_sort(self.dag))
        node_outputs = {}

        for node_id in execution_order:
            node_data = self.dag.nodes[node_id]
            
            # 推送节点状态：运行中
            if websocket:
                await websocket.send_json({"node_id": node_id, "status": "running"})
                
            # 执行具体逻辑（调用路由、大模型或渲染器）
            result = await self._execute_node(node_data, node_outputs)
            node_outputs[node_id] = result
            
            # 推送节点状态：完成
            if websocket:
                await websocket.send_json({"node_id": node_id, "status": "completed", "result": result})

        return node_outputs

    async def _execute_node(self, node_data, context):
        # 匹配对应模块，如 app.renderers.comfyui 或 app.ai.client
        pass

```

### 2.3 补充 WebSocket 路由 (`backend/app/main.py`)

对于画布节点式流转，轮询 REST API 效率太低。建议在 `main.py` 或 `agent/routes.py` 中补充 WebSocket 接口，实现执行过程的流式打点反馈。

```python
from fastapi import APIRouter, WebSocket, WebSocketDisconnect
from app.agent.engine import WorkflowEngine
from app.agent.types import WorkflowGraph

router = APIRouter()

@router.websocket("/ws/workflow/execute")
async def execute_workflow_ws(websocket: WebSocket):
    await websocket.accept()
    try:
        # 接收前端的 JSON 拓扑图
        data = await websocket.receive_json()
        graph = WorkflowGraph(**data)
        
        engine = WorkflowEngine(graph)
        # 执行工作流，实时反馈节点状态
        results = await engine.execute(websocket=websocket)
        
        await websocket.send_json({"status": "workflow_finished", "final_output": results})
    except WebSocketDisconnect:
        print("Client disconnected")
    except Exception as e:
        await websocket.send_json({"status": "error", "message": str(e)})

```

---

## 3. 业务层模块衔接

### 3.1 渲染器对接 (`backend/app/renderers/comfyui.py`)

目前的渲染器代码可以直接封装为一个特殊的 `RenderNode` 执行器。当前端提交的节点 `type` 为 `comfyui_render` 时，`_execute_node` 会将其 `data` 组装为底层的 prompt API payload 发送给渲染服务，并异步等待渲染完毕。

### 3.2 提示词动态提取 (`backend/app/prompt_learning/`)

允许在画布中将 `Prompt Template Node` 作为一个前置节点。它可以调用 `prompt_learning/retriever.py`，获取最新的经验/规则注入到 Prompt 字符串中，然后通过边 (Edge) 传递给 `LLM Engine Node` 进行推理。

---

## 4. 后续迭代建议

1. **本地存储与快照**：在初期阶段，为了快速迭代，可以将用户的画布 JSON 状态序列化后保存在本地存储中，或者直接以 `.json` 格式导出导入（类似短视频矩阵辅助工具中的自动化流程配置文件）。
2. **对话生成画布**：进阶玩法是，在 `ChatPanel` 接收用户输入后，利用大模型自动生成一份 `WorkflowGraph` 的 JSON 结构渲染到右侧，用户基于生成的草图进行二次连线和修改。
"""

with open('/mnt/data/LumiWeave_Source_Code_Improvement.md', 'w', encoding='utf-8') as f:
f.write(md_content)

```

阿勇，这份针对 LumiWeave 源码级别改进的开发文档已经生成完毕。

文档中详细列出了前端 React Flow 库的引入与组件拆分方法，以及后端如何利用 `networkx` 构建 DAG（有向无环图）执行引擎，并配置 WebSocket 以实现类似大模型及 AI 渲染（如我们常用的那些节点式工作流）的实时状态推流。

Your Markdown file is ready
[file-tag: code-generated-file-1c94c309-65cf-4c43-9dbf-683ef83ff11d]

你可以结合目前后端现有的 `agent/router.py` 和 `renderers/comfyui.py` 等模块，参照文档中的架构示例逐步将其改造为支持可视化编排的形态。如果在这中间涉及到接口封装或者节点数据流绑定的具体代码实现，随时可以继续探讨。

```