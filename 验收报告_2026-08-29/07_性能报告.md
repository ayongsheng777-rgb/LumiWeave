# 07 性能报告 — 绵绣 LumiWeave

> Phase 9：性能验收（实测）｜ 评分维度权重 10%

## 一、API 响应（实测）

| 指标 | 结果 | 评价 |
|---|---|---|
| 健康检查 | 3.7ms | 优 |
| 常规 CRUD 端点 P50 | 4-8ms | 优 |
| 90 天 Token 聚合 | 23ms | 优（791 行日志级别） |
| 20 并发 GET /api/scenes | 全 200，无失败 | 通过 |
| 场景对象批量写（历史实测） | 1000 对象 0.98s（1022/s）；300 对象 0.34s | 优 |
| 千对象全量读回 | 0.05s | 优 |

## 二、AI 调用性能（token_usage_log 实测统计）

| 模型 | 调用数 | 成功率 | 平均延迟 |
|---|---|---|---|
| zai-org/GLM-5.2 | 158 | 98.7% | 21.5s |
| deepseek-ai/DeepSeek-V3 | 105 | 99.0% | 11.8s |
| Qwen/Qwen-Image（出图） | 23 | 87.0% | 34.6s |
| 其余（Kimi/Nex 等） | 24 | 100% | 3.6-7.2s |

- LLM 延迟 4-21s 属云端推理正常区间；出图 34.6s 正常
- 整体失败率 15.3%（121/792），但**近日（08-29）scene_action 全成功**——失败集中早期配置调试期，非系统性问题

## 三、资源占用（docker stats 实测）

| 容器 | CPU | 内存 |
|---|---|---|
| backend | 0.13% | 75MB |
| frontend (nginx) | 0.00% | 10MB |
| mcp | 0.16% | 68MB |
| postgres | 1.90% | 32MB |
| redis | 1.76% | 9MB |

全栈 < 200MB 内存，32GB 主机毫无压力。

## 四、前端性能

| 项 | 现状 | 评价 |
|---|---|---|
| 主 bundle | **1.99MB 单文件，未见 gzip 压缩头** | P2：首屏慢，建议 nginx 开 gzip + vite 分包 |
| 页面 title/资源加载 | 正常（hash 指纹 CRChTsxX） | ✅ |
| 前端渲染 | 历史 puppeteer 实测 15 节点画布无报错 | ✅ |
| React 死循环风险 | error #185 已在 V2.5 修复（selector 精确订阅） | ✅ 已修 |

## 五、已知性能设计

- 本地渲染 asyncio.Queue(maxsize=10) + 常驻 worker 串行消费防爆显存 ✅
- AI 动作支持真异步（asyncio.create_task + tasks 进度轮询）✅
- 防抖落库（patchObject / persistGeometry）✅

## 六、结论

后端性能**优秀**，无瓶颈；唯一明显短板是前端 2MB 未压缩单 bundle。**性能单项得分：85/100**。
