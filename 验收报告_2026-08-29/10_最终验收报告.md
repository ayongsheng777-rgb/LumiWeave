# 10 最终验收报告 — 绵绣 LumiWeave

> 验收日期：2026-08-29 ｜ 验收依据：《AI软件验收智能体规范 V1.0》
> 验收方式：代码审计（2 路并行）+ 容器内实测 + 真实 API 调用 + 数据库直查，全部结论有实测证据

## 一、综合评级

# 🟡 C 级 — 存在明显风险（P0 修复前不可对外交付）

| 维度 | 权重 | 得分 | 加权 |
|---|---|---|---|
| 架构 | 20% | 82 | 16.4 |
| 功能 | 25% | 80 | 20.0 |
| 稳定性 | 15% | 72 | 10.8 |
| 安全 | 15% | **45** | 6.75 |
| 性能 | 10% | 85 | 8.5 |
| 体验 | 10% | 80 | 8.0 |
| 文档 | 5% | 85 | 4.25 |
| **合计** | 100% | — | **74.7** |

加权分 74.7 本在 B 级下沿，但因存在**已实测复现的 P0 安全漏洞**（/uploads 路径穿越可接管全系统），按规范「P0 = 安全漏洞/系统不可上线」**降级定为 C 级**。
**修复 P0×2 + P1×4（安全类）后，预计可回升至 B+（85 分左右）。**

## 二、一句话结论

> 这是一个**设计水平、功能完成度、AI 能力都明显高于同类个人项目**的成熟系统——架构有契约、有门面、有注册表，功能有 30 天 792 次真实 AI 调用数据支撑；但它现在**大门没锁**（一个免鉴权静态路由把整个 OTP 认证体系架空），且主聊天入口因配置失效不可用。锁好门、修好聊天，就是好系统。

## 三、必须立即处理（按优先级）

1. **B01 /uploads 路径穿越**（P0，10 分钟可修）：resolve 校验 + 拒绝 `..`
2. **B02 python-multipart 升级 ≥0.0.27**（P0，改一行版本号 + 重建镜像）
3. **B06 PG/Redis 端口绑回 127.0.0.1**（P1，改 compose 两行）
4. **B04 MCP 权限强制**（P1，工具分发层加 has_permission）
5. **B05 登录限流**（P1）
6. **B03 主对话模型配置修复**（P1，功能项：active profile 指向有 key 的硅基流动配置）
7. **B15 轮换 .env 里的 MiniMax Key**（P2，因库内与文件均有明文痕迹）

## 四、项目亮点（值得保持）

- 统一 DAG 协议 / 服务门面层 / 注册表驱动 / 统一 Task 与错误码 —— 正确的大型系统做法
- Token 计费全链路可追溯（task_id/workflow_id/node_id/cost），30 天 ¥9.12 实测
- MCP 40+ 工具双模式，外部 AI 智能体可完整操控画布 —— 差异化能力
- 幂等建表、索引到位、参数化 SQL 零注入点、api_key 脱敏完整
- AGENTS.md 维护手册 27 个迭代章节全程留痕，踩坑记录详实 —— 文档文化优秀

## 五、中长期建议（商业化前）

- 补测试体系（修 test_core.py + pytest 入依赖 + 核心引擎用例），告别纯手工 verify 脚本
- 拆 scene/actions.py（2144 行）与前端三 store 单一事实源
- 接通 task_events/task_results 或裁剪该设计；清理 Redis 死依赖
- 数据库备份方案 + AI Key 加密存储
- 前端 gzip + 分包；失败调用聚合视图

## 六、验收过程记录

- 代码审计：后端全模块 + 前端 src + 依赖清单（2 路并行独立审计，结论交叉验证）
- 线上实测：认证（正/反）× 4、核心端点 × 11、功能闭环 × 4（工作流/场景/MCP/权限）、安全复现 × 4（穿越/权限/弱口令/未授权）、性能（并发/延迟/资源）
- 数据库直查：27 表行数 + 索引 + 调用明细 + 配置落库内容
- 测试产生的临时数据（场景/工作流/MCP 客户端）已全部清理

---
*验收智能体：WorkBuddy ｜ 报告目录：验收报告_2026-08-29/（01-10 共 10 份）*

---

# 附：修复复核（2026-08-29 当日完成）

**已修复并实测验证（验收脚本 16 项 + pytest 8/8 全绿）**：
- ✅ B01 /uploads 穿越（_safe_join 校验，curl --path-as-is 复核 404）
- ✅ B02 python-multipart 0.0.27
- ✅ B04 MCP 权限强制（只读 token 调写/执行工具 403，tools/list 200）+ 权限映射补全 film/scene/marketing 40+ 工具
- ✅ B05 登录限流（第 6 次错误 OTP 触发 429）
- ✅ B06 PG/Redis 绑 127.0.0.1
- ✅ B07 assets/dir 守卫（/etc、盘符根均 400）
- ✅ B03 更正：系验收脚本字段名误用；已补空消息 400 校验
- ✅ B09 test_core.py 修复 + pytest 入依赖，8/8 全绿
- ✅ B13 渲染 WS 补 token 校验｜B14 SSRF 拦截（169.254.169.254 实测被拦）｜B16 gzip｜B17 CORS｜B18 字段兼容｜B19 清理｜B20 文档
- ✅ 另按用户反馈修复影视拉片链路 8 项（purpose 统一、选分镜写 prompt、参考图合并连线素材、道具解析吞场景行、道具描述、配音节点 audio_type、图片 size 回写、PURPOSE_OPTS/labels）

**遗留（有理由暂不修）**：B08 actions.py 2144 行拆分（大重构，列入下批）；B10 Redis 死依赖暂留；B11 task_events 写入接通待设计确认；B12/B15 密钥加密与轮换需用户参与（MiniMax Key 请手动轮换）。

**复核后评级：B+（约 85 分）**——P0/P1 安全项全部闭环，剩余为架构债务与体验优化。

