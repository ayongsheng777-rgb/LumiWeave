# LumiWeave V2.5 规格书任务清单（断点续做对照表）

> 版本：2026-08-27（第五轮深度增强后 —— **P0/P1/P2 全清 + 深度增强全完成**）
> 依据：`D:/LumiWeave V2.5 专业场景画布重构实施规格书.md`（84 节，非 58 节）
> 覆盖审计：`tmp/scene_spec_coverage.md`（子代理逐节比对产出）
> 使用方式：**三阶段（P0/P1/P2）+ 深度增强已全部完成，本清单全勾。验收方法见下方每项「验收」栏；新需求按「七、断点续做 SOP」继续。**

---

## 一、总览（84 节）

| 维度 | 第一轮 | 第二轮 | 第三轮 | 第四轮 | 第五轮后（深度增强） |
|---|---|---|---|---|---|
| ✅ 完全达标 | 19% | 约 45% | 约 60% | 约 85% | **约 95%** |
| 🟡 部分/骨架 | 48% | 约 40% | 约 33% | 约 15% | 约 5% |
| ❌ 未实现 | 26% | 约 15% | 约 7% | 约 0% | **0%** |
| P0 / P1 / P2 缺口 | 6/—/— | 0/7/— | 0/0/6 | 0/0/0 | **0/0/0** |

已提交：`a5d031f` + `c44b82c` + `c6b1a13` + `3cd8467` + `63739a0`（第五轮深度增强）

---

## 二、P0 —— 全部完成 ✅（不再处理）

- [x] P0-01 Scene Engine（§5/§6/§7/§33/§34）—— `backend/app/scene/`，三表 CRUD + 注册表驱动
- [x] P0-02 Object Model（§9-§12）—— 20 类专业对象元数据，单节点组件 `sceneObject`
- [x] P0-03 Canvas Core 升级（§4/§31/§33/§34/§40）—— 三态画布 + 防抖落库 + 动态工具条/Inspector
- [x] P0-04 Scene Template（§39/§40）—— 3 场景即模板，注册表可扩展
- [x] P0-05 电商商品营销（§25/§26/§67）—— 商品分析/主图/场景图/海报/**详情页**/批量 SKU
- [x] P0-06 影视拉片（§14/§15/§59/§68）—— **自动拆镜**（ffprobe+ffmpeg+抽帧+视觉分析）
- [x] P0-07 Timeline（§30）—— SceneTimeline 按场号-镜号铺轨
- [x] P0-08 AI Canvas Command（§19/§21/§22）—— runAction 分发 + 动作执行
- [x] §32 Undo/Redo —— 50 步历史栈 + Ctrl+Z/Ctrl+Shift+Z + syncCanvas 回写后端
- [x] §18 Context Menu —— 节点右键（复制/主动作/锁定/删除）
- [x] §35 版本管理 —— scene_versions 快照/恢复
- [x] §37/§38 素材库 —— assets 表 + scene_id，生成结果自动登记 + 素材页签

**P0 验收（§66）逐项核对**：创建/保存/加载/删除场景 ✅ 刷新不丢 ✅ 对象创建/拖动/缩放/删除/复制 ✅ Undo/Redo ✅ Inspector ✅ AI Action ✅ Workflow 执行 ✅ Task 状态 🟡（底层 tasks 表在，场景动作未全部接 task） Result 回写 ✅ Asset 保存 ✅ Token 记录 🟡（AI 主链路有，场景动作侧未逐条记 token）

---

## 三、待办任务（断点从这里接着做）

### 阶段 1：P1 —— 应完成，明显影响体验（建议顺序）

- [x] **P1-01 短剧配音/字幕/成片合成（§69）**
  - 现状：短剧剧情/人物/场景/分镜/镜头/图片/视频已有；配音、字幕、最终成片无
  - 做法：`actions.py` 加 `generate_voice`（TTS Provider，如硅基流动 tts）/ `generate_subtitle`（LLM 按分镜生成台词字幕）/ `compose_final`（ffmpeg 合成成片，视频+音频+字幕）；注册到 ecommerce-drama 场景 actions
  - 验收：电商短剧场景可一键「剧情→分镜→镜头→配音→字幕→成片」

- [x] **P1-02 MCP Scene Tools（§41 / 八十二 P1-03）**
  - 现状：`app/mcp/` 已有 21 工具，但无 scene.* 工具
  - 做法：MCP 扩展 `scene.list / scene.create / scene.load / scene.save / scene.action.execute / scene.asset.list`，复用 `app/scene/service.py`
  - 验收：Codex/Claude 通过 MCP 调用场景工具可建场景/执行动作

- [x] **P1-03 Skill 桥接（§42）**
  - 现状：`app/skills/` 存在，场景动作未显式调用
  - 做法：`execute_action` 支持 `skill:` 前缀动作 → 调 skill runtime；场景对象数据注入 skill 上下文
  - 验收：场景内可触发已安装 skill

- [x] **P1-04 RAG 检索注入（§43）**
  - 现状：`prompt_knowledge` 表在（Phase 7 预留），无场景侧检索
  - 做法：prompt 生成/卖点提炼前按对象字段检索知识库，命中片段注入 LLM 上下文
  - 验收：详情页/分镜生成带知识库参考

- [x] **P1-05 对象级状态系统 + Task 关联（§52/§53）**
  - 现状：仅 busy 遮罩；生成对象无 idle/queued/running/completed 徽标
  - 做法：sceneStore 加 `objectStatus: Record<id,string>`，runAction 时对目标对象置 running→completed；后端可写 tasks 表关联 scene_id
  - 验收：对象角标显示状态，杜绝无限 Loading

- [x] **P1-06 批量任务进度 UI（§54）**
  - 现状：✅ 已做前端计数版——runAction 目标对象显示「执行中/完成/失败」徽标，AI 页签显示批量成功/失败计数
  - 做法：批量拆 Task 入队 + 前端进度组件（成功/失败/运行中 计数）
  - 验收：批量 SKU 时显示成功/失败计数（真异步 3/100 进度条留 P2-06 任务异步化）

- [x] **P1-07 场景动作 Token/Cost 落库（§57 补全）**
  - 现状：AI 主链路 token_usage_log 有记录；场景动作（chat/chat_json/出图）未逐条记
  - 做法：`actions.py` 的 `_llm_json/_llm_text/_gen_image` 包一层 usage 记录（model/provider/task_id=scene_id）
  - 验收：Token 面板能看到场景动作消耗

### 阶段 2：P2 —— 后续扩展

- [x] **P2-01 模板文件化与 12 类内置电商模板（§26/§39/§40）**
  - 做法：`backend/app/scene/templates.py` 抽 JSON 模板（电商 12 类：主图/场景图/海报/详情页/9:16 视频等）
- [x] **P2-02 响应式布局收口（§49/§50）**
  - 做法：小屏 Inspector/BottomBar 改 Bottom Sheet；统一圆角/阴影/动画设计 token
- [x] **P2-03 商业化套餐与 Provider 定价管理（§73/§74）**
  - 做法：Free/Pro/Business 限制 + Provider Pricing/并发/超时管理 UI（model_pricing 表已在）
- [x] **P2-04 顶层产品菜单（§75）**
  - 做法：首页 + 创作/项目/素材/AI/系统 菜单，承载六类创作入口
- [x] **P2-05 影视 Vision 全字段强化（§68 深度）**
  - 现状：拆镜可用，视觉分析为 best-effort（chat 带图链接）
  - 做法：接专用视觉 Provider（qwen-vl-max 等）逐帧出结构化字段，景别/运动/构图识别率提升
- [x] **P2-06 真异步批量进度 + 性能压测（§70-§72）**
  - 现状：✅ batch_generate 后台执行 + tasks.done/total 进度 + 前端轮询；压测 300 对象写入 0.34s（883/s）、读回 310 对象
  - 后续增强：千/五千对象画布实测、AI 动作全异步化（当前单动作仍 HTTP 同步等待）

---

## 四、已完成能力速查（防重复实现）

| 能力 | 位置 | 说明 |
|---|---|---|
| 场景注册表 | `backend/app/scene/registry.py` | 唯一真源，3 场景 + 20 类对象，前端零改动扩展 |
| 场景 CRUD/动作 | `backend/app/scene/routes.py` | `/api/scenes`：CRUD/types/templates/objects/edges/actions/analyze/generate/batch/versions/assets/film/* |
| 动作执行 | `backend/app/scene/actions.py` | chat_json/chat/cloud_image_generate/cloud_video_generate + best_provider；已注册 15+ 动作 |
| 影视拆镜 | `backend/app/film/breakdown.py` | ffprobe + ffmpeg scene 检测 + 抽帧 + 视觉分析降级；**依赖镜像内 ffmpeg** |
| 版本管理 | `service.create_version/list_versions/restore_version` | scene_versions 表，快照覆盖 objects/edges/data |
| 素材库 | `service.add_asset_for_scene/list_scene_assets` | 复用 assets 表（scene_id 列），出图/生视频/抽帧自动登记 |
| 前端状态 | `frontend/src/store/sceneStore.ts` | 历史栈/复制/素材/版本/防抖落库/几何持久化 |
| 画布 UI | `frontend/src/scene/*`（8 组件） | Canvas/Sidebar/Toolbar/Inspector/ObjectNode(右键菜单)/BottomBar(素材库)/Timeline |

## 五、断点续做 SOP

1. 看「三、待办任务」从第一个 `[ ]` 开始（建议按 P1 顺序）
2. 后端改 py → `docker compose build backend && up -d --force-recreate --no-deps backend`（无 bind mount）
3. 加表 → `docker compose exec -T postgres psql -U lumiweave -d lumiweave -f /docker-entrypoint-initdb.d/01_init.sql`（幂等）
4. 前端改完 → `npm run build`（先 `export NODE_OPTIONS=""` + `npm_config_cache=D:/tmp/lw_npm_cache`）→ `docker compose build frontend && up -d --force-recreate --no-deps frontend`
5. 验收 → 容器内跑 `tmp/verify_scene_engine.py` / `tmp/verify_scene_v25.py` / `tmp/verify_scene_p1.py` / `tmp/verify_scene_p2.py`（真实 TOTP 登录，`--force-recreate` 后脚本要重 `docker cp`）
6. 完成后更新本清单勾选与总览数字 → commit + push（代理 `-c http.proxy=http://127.0.0.1:1080`）

---

## 六、深度增强（2026-08-27 全部完成，commit 63739a0）

- [x] **增强#1 影视 Vision 专用 Provider**——`app/film/vision.py`：优先探测带视觉能力的 LLM Provider（vl/vision/qwen/gpt-4o/gemini）走 OpenAI 兼容 `image_url` 多模态，无则回退 chat_full 带图；二次清洗只保留镜头语言字段。**验收**：上传视频拆镜后，镜头卡字段（景别/运动/构图/光线/色调/人物/情绪）自动填充，实测识别出「中景/三分法/自然光/暖调」。
- [x] **增强#2 RAG 真向量检索**——`_embed()` 调 embedding Provider（best_provider→/embeddings），`_rag_retrieve` 余弦相似度优先（阈值 0.3），无向量回退拆词 ILIKE（修复整句反向匹配 bug）。**验收**：知识库录入后，详情页/剧情生成自动注入相关知识。
- [x] **增强#3 AI 动作全异步**——`execute_action` 支持 `parameters.async_mode` → 后台 asyncio 执行 + tasks 进度；前端 AI 页签「异步执行」开关 + 轮询。**验收**：勾选异步执行后点动作，立即返回、可继续操作画布，完成后自动刷新。
- [x] **增强#4 套餐接入配置**——plans 存 `app_kv`，`POST /api/scenes/plans` 可切换四档套餐，配额按当前套餐算；顶栏菜单每档可「启用」。**验收**：顶栏 ☰ → 系统·套餐 → 点某档「启用」，场景数上限随之变化。
- [x] **增强#5 千对象压测**——1000 对象写入 **0.98s（1022/s）**、全量读回 **0.05s**。**验收**：见 `tmp/verify_scene_deep.py`。
- [x] **增强#6 响应式全量**——窄屏（<1100px）Inspector 变底部抽屉、侧边栏初始收起。**验收**：缩窄浏览器窗口观察布局自适应。

## 七、断点续做 SOP
